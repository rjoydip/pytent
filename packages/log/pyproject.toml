[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pytent-log"
version = "0.0.0"
description = "Logging for Pytent"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pytent_log"]

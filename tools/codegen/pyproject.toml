[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pytent-codegen"
version = "0.1.0"
description = "Codegen for Pytent"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/init"]